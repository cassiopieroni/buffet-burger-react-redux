import styled from 'styled-components';

export const StyledMain = styled.main`
    margin: 0 auto;
    min-height: 100vh;
    min-width: 100%;
    font-family: 'Anton', sans-serif;
    position: relative;
`;